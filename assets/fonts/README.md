# Fonts

Place `.ttf`/`.otf` here. Suggested (open licenses, see `/ASSETS.md`):

- `display.ttf` — a mythic display face for titles (e.g. Cinzel, OFL)
- `ui.ttf` — a clean readable UI face (e.g. Inter or Noto Sans, OFL)

Wire them via a Godot Theme later; greybox currently uses the engine default.
