# Boon / blessing icons

Shown on the boon-choice buttons. Optional. Filename = blessing id, e.g.
`greece_zeus_chain.png`, `greece_athena_deflect.png` (ids in
content/<pantheon>/blessings.json). ~48–64 px, transparent.
